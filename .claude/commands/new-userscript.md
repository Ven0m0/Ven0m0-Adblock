---
description: Scaffold a new userscript using the new-userscript skill
argument-hint: <script name>
---

Use the `new-userscript` skill to scaffold a new userscript named `$ARGUMENTS`. If the name, match pattern(s), description, or GM grants aren't clear from the arguments, ask for them per the skill's instructions.
