[project]
name = "ven0m0-adblock"
version = "1.0.0"
description = "Ad-blocking filter list utilities"
requires-python = ">=3.13"
dependencies = [
  "aiohttp>=3.13.5",
  "aiofiles>=25.1.0",
]

[tool.uv]
managed = true

[dependency-groups]
dev = [
    "prek>=0.3.8",
    "ruff>=0.15.10",
]
