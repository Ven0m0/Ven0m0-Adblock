{
  "name": "ven0m0-adblock",
  "version": "1.0.0",
  "description": "Comprehensive ad-blocking and web experience enhancement project with filter lists and userscripts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "uv run python -m Scripts.build",
    "build:all": "uv run python -m Scripts.build adblock hosts userscripts",
    "build:adblock": "uv run python -m Scripts.build adblock",
    "build:hosts": "uv run python -m Scripts.build hosts",
    "build:userscripts": "uv run python -m Scripts.build userscripts",
    "update:lists": "uv run python -m Scripts.update_lists",
    "lint": "bun run lint:js && bun run lint:filters && bun run lint:md",
    "lint:js": "bun run lint:biome && bun run lint:oxlint",
    "lint:biome": "bun x biome lint .",
    "lint:oxlint": "bun x oxlint --quiet .",
    "lint:filters": "bun x @adguard/aglint",
    "lint:yaml": "command -v yamllint >/dev/null && yamllint . || { echo 'yamllint not installed. Install with: pip install yamllint' >&2; exit 1; }",
    "lint:md": "bun x markdownlint-cli2 '**/*.md' '#node_modules' '#.opencode' '#.kilo' '#.venv'",
    "lint:aglint": "bun x @adguard/aglint",
    "lint:fix": "bun x oxlint --fix . && bun x biome check --write . && bun x @adguard/aglint --fix",
    "lint:fix:all": "bun run lint:fix && bun run format",
    "format": "bun x biome format --write .",
    "format:check": "bun x biome format .",
    "test": "bun run lint && bun run format:check",
    "test:ci": "bun run lint && bun run format:check",
    "clean": "rm -rf node_modules dist lists/releases userscripts/dist Filters",
    "clean:cache": "rm -rf node_modules/.cache",
    "clean:all": "bun run clean && bun run clean:cache",
    "precommit": "bun run lint:fix:all",
    "validate": "bun run test && bun run build",
    "aglint": "bunx @adguard/aglint --cache --cache-location .aglintcache --cache-strategy content \"**/*.txt\""
  },
  "devDependencies": {
    "@adguard/aglint": "^3.0.2",
    "@adguard/dead-domains-linter": "^1.0.33",
    "@adguard/hostlist-compiler": "^2.1.0",
    "@biomejs/biome": "^2.5.6",
    "@types/bun": "^1.3.14",
    "esbuild": "^0.28.1",
    "markdownlint-cli2": "^0.23.2",
    "oxlint": "^1.76.0",
    "terser": "^5.49.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "bun": ">=1.3.11"
  },
  "packageManager": "bun@1.3.11",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ven0m0/Ven0m0-Adblock.git"
  },
  "bugs": {
    "url": "https://github.com/Ven0m0/Ven0m0-Adblock/issues"
  },
  "homepage": "https://github.com/Ven0m0/Ven0m0-Adblock",
  "keywords": [
    "adblock",
    "adblocker",
    "ad-blocking",
    "filter-lists",
    "hostlist",
    "hosts-file",
    "privacy",
    "security",
    "userscripts",
    "web-enhancement"
  ],
  "author": "Ven0m0",
  "license": "GPL-3.0",
  "overrides": {
    "axios": ">=1.15.0",
    "follow-redirects": ">=1.15.11",
    "tldts": "7.4.10",
    "tldts-core": "7.4.10"
  }
}
